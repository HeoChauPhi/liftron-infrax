{% extends 'layout.twig' %}

{% block content %}
  {% if title_option != "on" %}
    {% if post.title %}
    <h1 id="page-title">
      {{ post.title }}
    </h1>
    {% endif %}
  {% endif %}

  {{ post.post_content }}
  
  {#{ function('flexible_content', 'group_component') }#}
{% endblock %}
